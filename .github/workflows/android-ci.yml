name: Android CI

on:
  # 添加 push 触发条件，监控 LibRyujinx.csproj 的变更
  push:
    branches: [ test ]
    paths:
      - 'src/LibRyujinx/**'   # 监控 LibRyujinx 目录下的所有文件
      - 'Directory.Packages.props'  # 监控中央版本管理文件
  workflow_dispatch:
    inputs:
      shouldrelease:
        description: "Create release"
        type: boolean
        required: false
        default: false
      uploadbuild:
        description: "Upload build artifact"
        type: boolean
        required: false
        default: true
      releasetag:
        description: "Release tag name"
        type: string
        required: false
      releasetype:
        description: "Mark as prerelease"
        type: boolean
        required: false
        default: false
      islatest:
        description: "Mark as latest release"
        type: boolean
        required: false
        default: true

env:
  ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
  NDK_VERSION: "25.1.8937393"
  CMAKE_VERSION: "3.22.1"
  DOTNET_VERSION: "9.0.101"
  JAVA_VERSION: "17"

jobs:
  build:
    name: Build and Package
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # ------------ 1. 代码检出 ------------
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0

      # ------------ 2. 环境设置 ------------
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      # ------------ 3. Android 环境配置 ------------
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: "latest"
          packages: |
            platforms;android-34
            build-tools;34.0.0
            ndk;${{ env.NDK_VERSION }}
            cmake;${{ env.CMAKE_VERSION }}
          accept-android-sdk-licenses: true

      # ------------ 4. 构建环境验证 ------------
      - name: Verify NDK Installation
        run: |
          echo "NDK路径: ${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}"
          ls -l ${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}/toolchains/llvm/prebuilt/linux-x86_64/bin/clang++

      # ------------ 5. 缓存优化 ------------
      - name: CCache setup
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ccache-${{ runner.os }}
          maxsize: 500M

      - name: Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      # ------------ 6. 构建准备 ------------
      - name: Fix permissions
        working-directory: src/RyujinxAndroid
        run: |
          chmod +x gradlew
          sudo sysctl -w fs.inotify.max_user_watches=524288
          ulimit -n 65536

      # ------------ 7. 核心构建步骤（新增 .NET 项目构建） ------------
      - name: Build LibRyujinx (AOT)
        working-directory: src/LibRyujinx
        run: |
          dotnet restore
          dotnet build -c Release /p:RuntimeIdentifier=linux-bionic-arm64
          dotnet build -c Release /p:RuntimeIdentifier=ios-arm64

      # ------------ 8. Android 应用构建 ------------
      - name: Build with Gradle
        working-directory: src/RyujinxAndroid
        env:
          ORG_GRADLE_PROJECT_ndkPath: ${{ env.ANDROID_SDK_ROOT }}/ndk/${{ env.NDK_VERSION }}
          CCACHE_DIR: /tmp/ccache
        run: |
          ./gradlew assembleRelease \
            --stacktrace \
            --no-daemon \
            -Pandroid.ndkPath=$ORG_GRADLE_PROJECT_ndkPath \
            -Dorg.gradle.workers.max=2 \
            -Dorg.gradle.caching=true

      # ------------ 9. 产物处理 ------------
      - name: Upload APK
        if: ${{ inputs.uploadbuild || github.event_name != 'workflow_dispatch' }}
        uses: actions/upload-artifact@v4
        with:
          name: ryujinx-android
          path: |
            src/RyujinxAndroid/app/build/outputs/apk/release/*.apk
          retention-days: 7

      - name: Create Release
        if: ${{ inputs.shouldrelease }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.releasetag || github.ref_name }}
          prerelease: ${{ inputs.releasetype }}
          generate_release_notes: true
          files: |
            src/RyujinxAndroid/app/build/outputs/apk/release/*.apk
