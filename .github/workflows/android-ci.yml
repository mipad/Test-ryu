name: Android CI (Linux Bionic ARM64)

on:
  push:
    branches: [ "test" ]
  pull_request:
    branches: [ "test" ]

env:
  GRADLE_USER_HOME: ${{ github.workspace }}/src/RyujinxAndroid/.gradle
  NDK_VERSION: "25.2.9519653"
  CMAKE_VERSION: "3.22.1"
  BUILD_TOOLS_VERSION: "34.0.0"
  DOTNET_VERSION: "9.0.101"
  ANDROID_HOME: ${{ github.workspace }}/android-sdk

jobs:
  build:
    name: Build Android Library
    runs-on: ubuntu-latest

    steps:
      # ================== [1] 代码检出 ==================
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GHH_PAT }}
          repository: mipad/Test-ryu
          ref: test
          submodules: false
          fetch-depth: 1
          sparse-checkout: |
            /*
            src/RyujinxAndroid/**
            !src/RyujinxAndroid/app/src/main/cpp/libraries/adrenotools
          sparse-checkout-cone-mode: false
          clean: true

      # ================== [2] 环境初始化 ==================
      - name: Prepare workspace
        run: |
          NDK_HOME="$ANDROID_HOME/ndk/$NDK_VERSION"
          echo "NDK_HOME=$NDK_HOME" >> $GITHUB_ENV
          mkdir -p "$ANDROID_HOME/cmdline-tools"

      # ================== [3] .NET环境配置 ==================
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          cache: false

      # ================== [4] Android SDK安装 ==================
      - name: Install Android Tools
        run: |
          cd "$ANDROID_HOME"

          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-*.zip -d cmdline-tools
          mv cmdline-tools/cmdline-tools cmdline-tools/latest
          rm commandlinetools-linux-*.zip

          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$PATH"
          echo "Current PATH: $PATH"
          which sdkmanager

          mkdir -p ~/.android && touch ~/.android/repositories.cfg
          yes | sdkmanager --licenses

          sdkmanager --verbose \
            "platform-tools" \
            "build-tools;$BUILD_TOOLS_VERSION" \
            "ndk;$NDK_VERSION" \
            "cmake;$CMAKE_VERSION"

          echo "NDK installation verification:"
          ls -l "$NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android30-clang++"

      # ================== [5] 配置项目文件 ==================
      - name: Configure Project
        working-directory: src/LibRyujinx
        run: |
          sudo apt-get install -y xmlstarlet

          # 修正 RuntimeIdentifiers（复数形式）
          xmlstarlet ed -L \
            -u '/Project/PropertyGroup/RuntimeIdentifiers' \
            -v 'android-arm64;linux-bionic-arm64' \
            LibRyujinx.csproj

          # 为 linux-bionic-arm64 添加独立链接参数
          for lib in "-llog" "-ldl" "-lz" "-lm" "-lc"; do
            xmlstarlet ed -L \
              -s '/Project/ItemGroup[contains(@Condition, "linux-bionic-arm64")]' \
              -t elem -n LinkerArg -v "$lib" \
              -i '/Project/ItemGroup/LinkerArg[text()="'"$lib"'"]' -t attr -n Include -v "$lib" \
              LibRyujinx.csproj
          done

          xmlstarlet val LibRyujinx.csproj || (echo "XML validation failed!"; exit 1)

      # ================== [6] 编译原生库 ==================
      - name: Build Native Library
        working-directory: src/LibRyujinx
        env:
          DOTNET_CLI_TELEMETRY_OPTOUT: 1
        run: |
          dotnet publish -c Release \
            -r linux-bionic-arm64 \
            -p:AndroidNdkDirectory=${{ env.NDK_HOME }} \
            -p:AndroidNativeLibraryName=libryujinx.so \
            -p:StripSymbols=true \
            --self-contained

          echo "Generated native libraries:"
          find bin/Release/net9.0/linux-bionic-arm64/publish -name '*.so'

      # ================== [7] 集成到Android工程 ==================
      - name: Integrate into Android Project
        working-directory: src/RyujinxAndroid
        run: |
          mkdir -p app/src/main/jniLibs/arm64-v8a
          cp ../LibRyujinx/bin/Release/net9.0/linux-bionic-arm64/publish/*.so app/src/main/jniLibs/arm64-v8a/
          
          echo "jniLibs directory contents:"
          ls -l app/src/main/jniLibs/arm64-v8a

      # ================== [8] 构建APK ==================
      - name: Build APK
        working-directory: src/RyujinxAndroid
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease \
            -Pandroid.ndkVersion=${{ env.NDK_VERSION }} \
            -Pandroid.sdkDirectory=${{ env.ANDROID_HOME }} \
            --stacktrace \
            --info

          echo "Generated APKs:"
          ls -l app/build/outputs/apk/release/*.apk

      # ================== [9] 上传产物 ==================
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-build-${{ github.run_number }}
          path: |
            src/RyujinxAndroid/app/build/outputs/apk/**/*.apk
            src/RyujinxAndroid/app/build/outputs/native-debug-symbols/**/*.so
          retention-days: 7
